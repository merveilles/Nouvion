# encoding: utf-8

module API
    class Memory
        def connect
            connect
        end

        def save(owner, key, value)
            save(owner, key, value)
        end

        def load(query)
            load(query)
        end
    end
end
